# This is wrong — explicit flags

Use this when something is **fucked** and you want it **on the record** (not buried in chat or drafts).

## How to flag

**One command** (from anywhere, paths are relative to `bride_of_charlie/`):

```bash
cd ~/workspace/agent-lab/projects/monuments/bride_of_charlie
uv run --project ../../../framework/deer-flow/backend python scripts/flag_issue.py "SHORT LABEL OF WHATS WRONG" --where "N-14 ep1 / Kent Frantzve / graph merge"
```

Or **edit this file**: add a bullet under **Open flags** (same idea as the command).

**Machine-readable log** (same events, for future automation): `canonical/name_issues.jsonl`

---

## Open flags

<!-- flag_issue.py appends below this line -->

---

## Resolved (move rows here when fixed)

| When flagged | What | Resolution |
|--------------|------|------------|
| | | |
