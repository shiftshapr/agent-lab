# Agent-lab — for JAUmemory

**Project:** agent-lab

**Summary:** Personal agent infrastructure. Shiftshapr = digital twin via Telegram (webhook, runs on MacMini via launchd). Opportunities ledger at `data/opportunities.json` (grants, deadlines). Protocols: daily-prep, weekly-preview, email-opportunity, slack-digest. Meeting reminders (15min → 5min if no ack). Drafting agent for Substack/grants. Publish via DeerFlow (Zoho Publish MCP). Context: `data/shiftshapr_context.json`. Audit: `logs/shiftshapr_audit.log`. Webhook: ngrok + `scripts/set-webhook-from-ngrok.py`.

**Tags:** agent-lab, project, shiftshapr, telegram, deerflow

---

To log to JAUmemory: authenticate first (`mcp_login` with your JAUmemory username/email), then use the `remember` tool with the content above.

**Bride of Charlie / workflow upgrades (Mar 2026):** full paste-ready log → [`JAUMEMORY_LOG_2026-03_Bride_Charlie_Workflow.md`](./JAUMEMORY_LOG_2026-03_Bride_Charlie_Workflow.md) (Neo4j ports, transcripts_corrected, validate/ingest/corrections, two-phase, Phase 1 truncation fixes, MiniMax OpenAI-compat note).
